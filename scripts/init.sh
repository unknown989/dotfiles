#!/bin/sh

git config --global init.defaultBranch main
git config --global credential.helper store

git config --global user.name "Brian Polier"
git config --global user.email "unknown989@protonmail.com"
